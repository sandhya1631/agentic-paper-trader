import { PagePlaceholder } from "@/components/page-placeholder";

export default function SettingsPage() {
  return (
    <PagePlaceholder
      title="Settings"
      description="Agent and account configuration, plus operator controls (Trigger Now / Kill Switch)."
      issue="US-4.1.3 (#34)"
    />
  );
}
