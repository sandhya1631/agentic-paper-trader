# Copy to `.env.local` and adjust as needed. `.env.local` is git-ignored.

# Base URL of the backend API (FastAPI). Must be prefixed with NEXT_PUBLIC_ to be
# readable in browser code. Defaults to http://localhost:8000 when unset.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
